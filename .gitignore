# Worktrees
.worktrees/

# Old GitLab .git, renamed aside during the fresh GitHub init (not tracked)
.git-gitlab-backup/

# Node
node_modules/
npm-debug.log*

# Build output
dist/
build/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Self-hosted GitHub Actions runner install (should live alongside this repo, not inside it —
# contains its own credentials/.runner config and, once jobs run, full _work/ checkouts)
actions-runner/

# Stray tool binary downloaded into the repo root while following runner setup docs
/kubectl
